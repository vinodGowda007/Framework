
namespace SeleniumAutoFramework.Base
{
    // Base Page class is extended by all object classes, thus inheriting all the base methods
    public class BasePage : Base
    {
        public BasePage(ParallelConfig parallelConfig, LoggingStep loggingStep) : base(parallelConfig, loggingStep)
        {

        }
    }
}
